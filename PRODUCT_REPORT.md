# 剪辑 Agent 初版网站产品报告

**评审日期：** 2026-09-23  
**评审范围：** 当前本地 Web 原型、后端生成流水线、已有样例成片  
**产品阶段判断：** 可演示的 MVP / 技术闭环原型，尚不适合直接公开上线

## 1. 执行摘要

剪辑 Agent 已经跑通了一个有明确价值的核心闭环：用户上传口播视频，系统自动完成转写、分镜、图形化包装、字幕、音效和竖屏渲染，最终可在网页内预览并下载成片。当前目录中的 7 份样例成片均为 1080×1920、H.264 + AAC，时长覆盖 17.27–81 秒，说明“从输入到可播放 MP4”的技术链路不是静态演示，而是已有真实产物。

初版最大的优点是聚焦：首屏只有输入方式、素材区和一个主按钮，用户几乎不需要学习。最大的风险也来自这种“全自动黑盒”设计：用户不能在昂贵的渲染前检查转写、修正文案、调整分镜或选择风格；一旦结果不满意，只能重新生成。样例抽帧还显示，成片存在信息层级过密、字幕断句不自然、标题与字幕同时抢占注意力等问题。

综合判断：

- **适合：** 内部演示、技术验证、邀请制小范围试用。
- **暂不适合：** 公开 Beta、多人并发、承诺稳定交付的商业使用。
- **下一阶段目标：** 从“能自动生成”升级为“用户能低成本得到可发布结果”。

## 2. 产品定位与目标用户

### 2.1 一句话定位

面向需要批量制作竖屏知识口播内容的创作者，将一段原始口播自动包装成带字幕、图解、转场和音效的短视频。

### 2.2 核心用户

1. **知识类个人创作者**：有稳定口播素材，但不会或不愿做后期。
2. **短视频运营人员**：需要把讲解素材快速加工成统一风格的日更内容。
3. **课程与企业内容团队**：需要将专家口述快速转成适合抖音、视频号、小红书等平台的竖屏片段。

### 2.3 核心任务

> “我给你一段口播，希望尽量不做剪辑操作，在可接受的等待时间内拿到一条能发布的竖屏视频。”

当前原型较好地覆盖了“少操作”和“拿到成片”，但对“可接受的等待”“结果可控”和“能直接发布”覆盖不足。

## 3. 当前产品能力盘点

| 模块 | 当前能力 | 判断 |
|---|---|---|
| 输入 | 上传视频或输入提示词 | 双入口清楚，但两种模式的产物能力并不对等 |
| 上传 | 拖放/点击选择，前端接受视频类型 | 操作简单；缺少时长、尺寸、编码、800 MB 上限等明确说明 |
| 转写 | 本地 Whisper，中文简繁转换，词级时间戳 | 技术路径合理，利于字幕高亮与本地处理 |
| 文案/分镜 | DeepSeek 可选；无密钥时使用启发式规则 | 有降级能力，但结果质量波动会较大 |
| 视觉包装 | hero、要点、步骤、对比、结构图、趋势图、结尾等模板 | 已形成基础模板系统，但风格单一、语义匹配仍偏规则化 |
| 字幕与音效 | 词级高亮字幕、whoosh/pop 音效 | 有短视频感；需要控制信息密度与音量一致性 |
| 渲染 | HyperFrames 逐帧渲染，输出 1080×1920 MP4 | 已有真实产物；耗时长且依赖本机工具链 |
| 任务反馈 | 阶段、百分比、日志、SSE 实时更新 | 比“无反馈等待”好；缺少 ETA、队列位置和自动重连 |
| 结果 | 网页预览、下载 | 最小交付闭环完整 |
| 恢复 | localStorage 记录一个任务 ID | 同一浏览器、同一服务进程内有效；服务重启后失效 |
| 项目管理 | 无历史记录、重命名、删除、再次编辑 | 仍是一次性工具，不是创作工作台 |

## 4. 体验评审

### 4.1 做得好的部分

- **价值表达直接。** 用户进入页面即可理解“上传口播，自动成片”。
- **首屏任务单一。** 没有账户、复杂参数或模板市场干扰首次体验。
- **操作成本低。** 拖入文件后只需点击“开始生成”。
- **等待过程有反馈。** 进度条、阶段和日志能降低长任务的不确定感。
- **结果交付完整。** 完成后可直接预览和下载，不需要跳转到其他工具。
- **视觉基调统一。** 深色背景、青色强调、圆角卡片与生成视频的视觉语言一致。

### 4.2 主要体验问题

#### A. 产品承诺与真实能力存在偏差

页面宣称会“配图解动效”，但当前系统主要生成固定模板图形，并没有真正的素材检索或语义配图。提示词模式也没有 TTS 或人物音轨：从构建逻辑看，没有上传视频时只会生成画面、字幕和音效，因此很可能得到没有旁白的成片。该入口目前不应与“上传口播视频”并列承诺同等结果。

建议二选一：

- 补齐提示词模式的文稿确认、TTS 配音和声音选择；或
- 暂时将其改名为“生成字幕动画草稿”，明确它不是完整口播成片。

#### B. 缺少渲染前的确认环节

当前流程是上传后直接进入 5–15 分钟渲染。用户无法预览或修改：

- 转写文本和错别字；
- 分句与字幕断点；
- 自动选择的场景模板；
- 标题、强调词和文案长度；
- 人像裁切位置、字幕安全区、音效开关。

这会把任何小错误都放大为一次完整重渲染。建议增加一个轻量的“生成方案确认页”，至少支持文本纠错、场景切换和字幕预览。

#### C. 成片信息密度偏高

样例首屏同时出现顶部标题、中部大标题和底部字幕，三个层级都使用大号粗体；视觉冲击力足，但读者注意力没有明确落点。结构图场景中，人物画中画、图解、场景标题和双行字幕同时存在，安全区紧张。

建议：

- 开场只保留一个主叙事焦点，字幕与主标题避免逐字重复；
- 内容场景限制为“人物 + 图解 + 字幕”中的两个强层级，第三层降级；
- 增加字符数、行数、底部安全区和遮挡检测；
- 对连续长场景增加节奏变化，避免同一构图停留过久。

#### D. 错误恢复较弱

SSE 连接报错后，前端只会关闭连接并重新启用按钮，没有告诉用户任务是否仍在后台运行，也没有重连按钮。任务数据仅保存在服务进程内，服务重启后，即使浏览器保留任务 ID 也无法恢复。

建议：持久化任务元数据，前端采用有限次数自动重连，并明确展示“任务仍在运行 / 已中断 / 可重新连接”。

#### E. 可访问性不足

模式切换和上传区使用可点击 `div`，不是原生按钮；键盘用户难以操作。错误、进度和完成状态没有 `aria-live`，输入框依赖 placeholder 而没有持久标签。正式发布前应补齐语义化控件、焦点样式和屏幕阅读器状态播报。

## 5. 技术与运营评审

### 5.1 当前架构的优点

- 本地 Whisper 转写，减少了对外部 ASR 的强依赖。
- DeepSeek 不可用时会回退到启发式分镜，原型不会完全失去规划能力。
- 渲染前会检查关键帧密度，并在需要时自动转码，考虑到了手机素材的实际差异。
- 通过串行队列避免多个渲染任务同时抢满 CPU，适合单机演示。
- 生成的资源和版本相对固定，利于复现。

### 5.2 上线阻断项

#### 安全与数据隔离

- `uploadId` 和客户端提交的 `videoName` 被用于拼接文件路径，缺少严格的 basename/目录边界校验；API 被直接调用时存在目录穿越风险。
- 上传只限制累计字节数，没有验证真实媒体格式、扩展名、时长、分辨率或恶意文件。
- 没有身份验证和任务归属；知道任务 ID 即可读取状态或下载视频。
- 上传、转写中间文件、工程文件和最终视频没有 TTL 或清理策略，磁盘会持续增长。
- 使用 DeepSeek 时，文稿会发往外部服务，但页面没有数据处理说明或用户知情提示。

#### 稳定性

- 任务只存在内存 `Map` 中，服务重启后全部丢失。
- 任务队列为单进程串行 Promise 链，没有队列长度、取消、超时、重试或进程隔离。
- 工程校验失败后仍继续渲染，可能把已知布局问题带入最终成片。
- 超过上传大小时直接销毁请求，但缺少明确错误响应和残留文件清理。
- 页面只能记住一个任务；新任务会覆盖旧任务的恢复入口。

#### 工程化

- 当前目录不是 Git 仓库，缺少可追踪版本历史https://github.com/caohch123/Piece。
- `package.json` 只有启动脚本，没有单元测试、集成测试、lint 或 CI。
- FFmpeg、FFprobe、Whisper 模型、Whisper CLI、浏览器以及 HyperFrames 均为运行依赖，但没有一键环境检查和部署文档。
- 渲染阶段通过 `npx --yes hyperframes@0.8.50` 启动；首次运行依赖网络/缓存，生产环境需要固定、预装并验证依赖。

## 6. 优先级路线图

### P0：公开试用前必须完成

| 工作项 | 目标 | 验收标准 |
|---|---|---|
| 修正提示词模式 | 消除能力与承诺偏差 | 有 TTS 和完整音轨，或明确降级为无旁白草稿 |
| 文件安全与数据隔离 | 防止越界读写和任务串读 | 所有路径使用服务端生成名称；校验媒体；任务有所有者；下载需授权 |
| 任务持久化与恢复 | 服务重启后任务可查询 | 任务元数据落库；页面可恢复多个任务；输出有 TTL |
| 渲染前确认 | 降低整条重做成本 | 可编辑转写、标题、字幕断句；可预览关键帧后再渲染 |
| 质量门禁 | 阻止明显坏片交付 | 严重校验错误终止；自动检查溢出、遮挡、空场景、无音轨 |
| 错误与重连 | 让用户知道任务真实状态 | SSE 自动重连；失败原因可读；支持安全重试 |

### P1：封闭 Beta 阶段

- 建立任务中心：排队、处理中、完成、失败、取消、重新生成。
- 提供 3–5 套差异明显的风格，而不只是同一视觉模板的内容变化。
- 支持字幕字号、位置、强调色、音效开关和人像裁切等少量高价值设置。
- 增加生成前预计耗时、队列位置和生成后质量反馈。
- 建立样例集和自动回归：不同视频比例、时长、无声、噪声、普通话/夹英文、长句、弱网和中断恢复。
- 记录每阶段耗时、失败原因、重试次数和资源占用。

### P2：形成可增长产品

- 品牌模板、Logo、字体、颜色和片尾配置。
- 多平台导出比例与安全区适配。
- B-roll/图片素材检索与授权记录。
- 多语言转写、翻译与配音。
- 团队工作区、审核、版本和批量生产。

## 7. 建议的核心指标

### 北极星指标

**每周成功下载且用户判断“可发布”的成片数。**

仅统计“生成成功”会掩盖质量问题；需要在下载后增加一个非常轻的“可直接发布 / 需要小改 / 不可用”反馈。

### 漏斗指标

1. 进入页面 → 选择文件/输入主题。
2. 提交生成 → 成功创建任务。
3. 创建任务 → 成功完成渲染。
4. 完成渲染 → 播放预览。
5. 播放预览 → 下载。
6. 下载 → 判断可发布。

### 质量与效率指标

- 端到端成功率、各阶段失败率。
- P50/P90 生成耗时与排队耗时。
- 首次生成即可发布率。
- 用户修改转写、标题和字幕断句的比例。
- 重新生成率与单个可发布成片平均渲染次数。
- 字幕溢出、无音轨、黑帧、人物遮挡等自动质检命中率。
- 7 日内再次生成率。

## 8. 综合评分

| 维度 | 评分（10 分制） | 说明 |
|---|---:|---|
| 价值主张 | 8 | 需求明确、节省剪辑时间的价值容易理解 |
| 首次使用体验 | 7 | 简洁直接，但缺少格式说明和生成前确认 |
| 功能完整度 | 6 | 上传模式闭环完整；提示词模式能力缺口明显 |
| 成片质量 | 6 | 已有可展示视觉；字幕与信息密度仍需质检和人工可控性 |
| 稳定性 | 4 | 内存任务、单队列、弱恢复，不适合稳定服务 |
| 安全与数据治理 | 3 | 文件路径、鉴权、清理、第三方数据说明均未补齐 |
| 工程化与可维护性 | 4 | 代码结构清楚，但缺测试、CI、版本库和环境文档 |

**总体建议：5.5/10。** 这是一个方向正确、核心链路真实的初版，而不是“只能看不能用”的概念页；但下一阶段不应优先堆更多动效，而应先补齐结果可控性、质量门禁、任务可靠性和安全边界。

## 9. 本次验证依据与边界

- 实际访问本地首页，HTTP 状态为 200。
- 实际验证了上传/提示词模式切换和空提示词错误提示。
- 阅读了前端、HTTP 服务、任务队列、转写、分镜、工程生成与渲染实现。
- 对现有 7 份最终视频读取了媒体元数据，全部为 1080×1920、H.264 + AAC。
- 抽取并检查了样例视频 3 秒、8 秒和 14 秒画面。
- 本次未重新提交耗时 5–15 分钟的新渲染任务，因此对新任务的实时耗时与成功率不作统计性结论。

